plsql.execute <<-SQL
 -- CREACION TABLESPACE SIN PUNTO Y COMA
 -- EJEMPLO CREATE TABLESPACE ...
 
 CREATE TABLESPACE mid_term DATAFILE 'df_mid_term.dbf' SIZE 20M

SQL

plsql.execute <<-SQL
 -- CREACION PERFIL SIN PUNTO Y COMA
 -- EJEMPLO CREATE PROFILE ...

 create profile exam LIMIT
 idle_time 5
 failed_login_attempts 2

SQL

plsql.execute <<-SQL
 -- CREACION USUARIO SIN PUNTO Y COMA
 -- EJEMPLO CREATE USER

CREATE USER ejercicios 
IDENTIFIED by oracle 
DEFAULT TABLESPACE mid_term
QUOTA UNLIMITED ON mid_term
PROFILE exam

SQL

plsql(:data).execute <<-SQL
 -- CREACION VISTA 1 SIN PUNTO Y COMA
 -- EJEMPLO CREATE VIEW ...

CREATE OR REPLACE VIEW view_1
AS 
     SELECT SALESMAN.NAME AS "salesman_name", CUSTOMERS.NAME AS "customer_name", SALESMAN.COUNTRY AS "country" FROM SALESMAN INNER JOIN CUSTOMERS 
     ON CUSTOMERS.SALESMAN_ID = SALESMAN.ID
     WHERE CUSTOMERS.COUNTRY = CUSTOMERS.COUNTRY

SQL

plsql(:data).execute <<-SQL
 -- CREACION VISTA 2 SIN PUNTO Y COMA
 -- EJEMPLO CREATE VIEW ...

 CREATE OR REPLACE VIEW view_2
 AS 
     SELECT CUSTOMERS.* FROM CUSTOMERS INNER JOIN SALESMAN 
     ON CUSTOMERS.SALESMAN_ID = SALESMAN.ID
     WHERE SALESMAN.COMMISSION > 0.12
     
SQL

plsql(:data).execute <<-SQL
 -- CREACION VISTA 3 SIN PUNTO Y COMA
 -- EJEMPLO CREATE VIEW ...
SQL

plsql(:data).execute <<-SQL
 -- CREACION VISTA 4 SIN PUNTO Y COMA
 -- EJEMPLO CREATE VIEW ...
SQL